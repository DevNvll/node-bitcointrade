{
  "name": "bitcointrade-wrapper",
  "version": "0.1.0",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "jest",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --single-quote --no-semi --write",
      "git add"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "node-fetch": "^2.1.2"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "jest": "^23.1.0",
    "lint-staged": "^7.1.3",
    "prettier": "^1.13.4"
  }
}
